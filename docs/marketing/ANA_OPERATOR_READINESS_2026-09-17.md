# Aña and operator bounded readiness check — 2026-09-17

Read-only architecture review, then parent-authorized deterministic status improvement. No live sends, model/provider requests, credential changes or deployment.

Architecture: api/chat.js and ana_social.js share anaSystemPrompt(loadMenu, anaBrand), including Cajita/catering quote/lead-time guidance. Instagram adds its own1200-character owner-training and1200-character knowledge context; website chat does not use that extra context. Instagram inbox tick separately enforces configured channels, complaint escalation, rates, identity and DM reply-window checks. Webchat lead capture requires contact information and happens after availability/budget gates. None of these code paths establishes that production webhook/token/ticks are currently healthy. Shared menu fallback is not proof every cited price is fresh; the old code comment claiming it always matches checkout is stronger than the fallback evidence.

Operator is owner-gated and read-only. Existing deterministic capabilities/marketing-status commands avoid model calls. Other operations questions use the model with grounded orders/delivery/rewards context. Browser speech input/output is not an operational action registry. No commands publish, send replies, refund, modify orders or answer Google reviews.

Implemented one bounded improvement in functions/api/hub/owner/operator.js: marketing status reuses existing loadSocialHeartbeat and returns observed timestamps, cron/owner source, recorded last success/error and counts. Speech text labels queue observation time and distinguishes a record from current execution or a specific successful delivery. A started-but-unfinished record is named started_not_completed, not a current Running claim. execution_health remains unverified. No write or new external action was added.

Tests: initial Ana brand/social/operator/leads group80passed (/tmp/anejo-ana-operator-readonly-tests.log). Updated test/money/operator-readiness.test.js9passed (/tmp/anejo-operator-heartbeat-tests.log), including an explicit fetch trap and DBwrite trap, cron timestamp read-back, owner-triggered distinction, stale/missing/unfinished evidence and existing owner authorization. Scoped ESLint, JS syntax and diff check pass. These are local deterministic tests, not production messaging/microphone verification.

Remaining high-value work: verify authorized live webhook/tick outcomes separately; decide whether website Aña should receive a narrowly scoped factual training subset; ensure stale/fallback menu evidence cannot be represented as guaranteed current price. Those changes are not implemented here. Do not enlarge customer-reply training indiscriminately with social layout guidance or infer new send permissions.
