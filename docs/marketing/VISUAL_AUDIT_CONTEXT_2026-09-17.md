# Saved visual audit source binding — local evidence

Migration0120 adds nullable audit_detail_json, audit_context_snapshot and auto_audit_required. Existing audits have no fabricated rubric/source metadata and need a new audit to qualify for future automatic scheduling or clean trust credit. Existing manual/legacy publication authority is unchanged; NULL marker means no new automatic-audit requirement is asserted.

social_audit.js exports ordered SQL context versions of active brand docs, training rules/examples and menu items using id+updated_at. Captured before judging, compared in the same conditional audit UPDATE with the existing media/caption snapshot. Source changes in flight return409. The stored detail selects actual rubric_version, observations, suggestions, input_coverage and score_meaning; unavailable audits have null details. original_design_snapshot remains exclusively the existing design/caption fingerprint.

Trust clean credit and planner automatic scheduling require current design/source snapshots and rubric anejo-visual-1. Automatic scheduling sets auto_audit_required=1. Role-gated explicit manual schedule/publish clears that marker. The publisher rereads automatic evidence before provider operations; stale/unavailable evidence refuses. Automatic reordering refuses rather than changing an audited carousel. Manual food-first reordering behavior remains unchanged. GET exposes computed audit_current and rubric detail; legacy fallback returns audit_current0.

Caption/media/reorder mutations clear all associated audit details/context. Inline scheduling/publishing preserves unchanged caption evidence and clears changed-caption evidence atomically. No authority switch or trust threshold was changed.

Validation:94 focused SQLite/behavioral tests passed (/tmp/anejo-audit-context-focused.log), scoped ESLint and diff check passed. Tests cover source mutation in flight, each of four source collections changing after audit, edit/reorder invalidation, unchanged manual legacy behavior and refusal to publish stale automatic evidence or reorder automatic media. Full initial candidate run2701/2714 passed (/tmp/anejo-audit-context-full.log); remaining old source pins/context-cap fixtures are assigned to other agents, parent owns final full run.

Limits: version binding relies on source writers bumping updated_at and immutable media keys; it is not a distributed lock held across Meta network calls. Changes made after the last pre-provider check cannot be atomically coordinated with an external publication. Compiled repository fallback changes are not represented by the four D1 version sets; the current visual judge refuses missing live source evidence. No provider calls, production migration or deployment performed in this subtask.
