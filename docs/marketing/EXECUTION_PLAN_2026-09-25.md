# Añejo marketing execution plan — September 25

## Outcome and authority

Implement Dayan’s full marketing-team vision. The authoritative request ledger is `USER_REQUEST_LEDGER_2026-09-17.md`; this file organizes execution and does not narrow that scope. Existing published carousels are preserved. Implementation, checks and gated deployment are authorized. Public posting, customer communications, charges and credential changes remain excluded unless specifically authorized. No vault writes.

## Truth and continuity

Report intentions, started actions, local verification, deployment and live verification separately. A score is not proof of semantic correctness. An agent report is not independent verification. Before saying an executor is running, inspect its current handle/status. Save a checkpoint before ending work; next executor must inspect Git and read current evidence before acting. Do not recreate an unfinished goal, alter goal storage or infer account allowance from a goal status.

## Work ownership and handoff

Root owns architecture, integration, acceptance and release. Delegate narrowly bounded implementation with explicit file ownership; review all outputs before shipping. Prefer existing code and tests over rediscovery. Do not choose a model override unless its supported model and effort are known and appropriate. Context-limited work packets should include this file, the relevant source files, acceptance criteria and exclusions, rather than the entire historical conversation.

### A. Reliable brand audit — active implementation

Problem: the September25 saved model reviews are7/7 but invent logo positions, background uniformity and redundant CTA recommendations. Evidence: `AUDIT_READBACK_2026-09-25.json`. Do not modify correct artwork to satisfy faulty suggestions.

- Registry agent owns only compiler, generated exact-image facts and registry tests.
- Root owns runtime image hashing, per-slide declaration delivery, evidence persistence, UI and integration tests.
- Review agent owns read-only review; root resolves findings.
- Required tests: known JPEG exact match; altered/replaced JPEG no inherited facts; actual current slide numbering after reorder; unused manifest copy never represented as rendered; byte changes during audit rejected; unknown source remains explicitly unknown; no new trust credit/publication; incomplete provider results remain fail closed.
- Acceptance beyond local tests: release checks, exact deployed revision, supported-browser review of source records, controlled provider semantic evaluation against real saved designs. Prompt changes alone do not establish model reliability. No repeated provider calls merely to obtain a pass.

### B. Reusable premium content production

Retain full-frame real food, authentic emblem, adaptive readable branding, original-preserving polish and shared-library access from Today/Create. Review current deterministic renderer before selecting a production strategy. Required evidence includes real input-to-derivative flow, accessible saved original, reusable template output, protected-food layout, fonts/glyphs and deployed CPU/memory limits. Prototype success is insufficient for unattended rendering. See `UNATTENDED_RENDERING_PROOF_2026-09-17.md`.

### C. Integrated marketing team

Trace Lead, strategist, analyst and intel inputs/outputs against shared brand/menu/training authority; verify receipts and failure handling. Turn private owner ideas into reviewable campaign briefs without silently activating them. Require bounded strategy-to-content-to-audit-to-owner-review flow. Track inquiries and actual paid-order attribution separately from reach and likes.

### D. Ana and voice operations

Verify comments, DM and webchat adapters plus durable errors/retries without sending test customer communications. Current heartbeat evidence is not delivered-reply proof. Google review ingestion/reply needs confirmed provider access and adapter work; private draft desk is not an integration. Voice operator needs actual microphone/TTS and action-registry acceptance; typed idea persistence alone is insufficient. Ask only for missing facts or required authorization, then continue independent work.

### E. Original market readiness and Google scope

Retain role/checkout/Creative Studio QA and SEO/Google backlog from the request ledger. Use normal supported browser access, stop on denial, and never reuse old permission/session files. Do not submit a live inquiry if it would send unauthorized customer/owner notifications merely for testing. Document realistic safe verification and any remaining proof gap.

## Release and completion

Inspect branch and unrelated changes; run risk-appropriate tests and required release gates. Verify exact deployed revision and live behavior separately. Existing evidence is in `RELEASE_EVIDENCE_2026-09-25.md`; do not promote its historical tests to current production proof. Keep trust automation off until each applicable gate has evidence. Completion requires a requirement-by-requirement audit of the full ledger, not simply another release.

## Checkpoint

2026-09-25: goal tool reports active after app continuation. Expected branch confirmed, clean before current audit changes. Previous conversational goal exchanges produced no Añejo implementation progress. Runtime audit grounding now being implemented; not yet tested, deployed or semantically accepted. Next action: integrate registry, validate source declarations against renderer, test byte replacement guards and inspect independent review.


### Audit grounding local evidence

Implemented v6: exact-byte26image registry; source records supplied separately from model judgments; no registered declaration for unknown hashes; source records retained on provider failure;24KiB bounded declaration input; R2 recheck after judging and at automatic publish/clean trust decisions. No change enables trust or public publication. File ownership delegation reviewed by root. Compiler declarations checked against pinned render.html and marketing-branding.js; they are reviewed render declarations, not independent OCR or immutable attestations.

Validation: `npm test`2,939passed (`/tmp/anejo-audit-v6-root.log`); `npm run lint`0errors/11existingwarnings (`/tmp/anejo-audit-v6-lint.log`); Pages Functions build passed (`/tmp/anejo-audit-v6-build.log`); `git diff --check` passed. Fullroot run includes actual JPEG hash/mutation/order tests and automatic publish/trust guards. No Studio source changed in this increment. No provider call, customer send, public post or production mutation performed for these tests.

Open: release/deployment/live semantic acceptance. Hash rechecks are decision-time checks, not atomic R2/SQL locking; future immutable media pipeline remains necessary for stronger guarantees. Renderer registry currently covers reviewed revision3 exports only. Generic future template receipts and reliable model explanations remain incomplete. Keep goal active.


### Resume checkpoint after PR132

Released226a03a and live source-record display verified. See release evidence and AUDIT_V6_READBACK_2026-09-25.json. Choice now6/7/nullscore with a misapplied product-fidelity unknown; do not equate fail-closed behavior with semantic correctness. No second provider retry performed. Goal remains active. Fullfuture receipt proposal was inspected by root and saved as FUTURE_RENDER_RECEIPTS_2026-09-25.md; it is a proposal, not implementation. Next executor must inspect Git/current goal state, preserve current provider results and fix structured product-claim scope before any trust enablement. Existingbrowser tab1238612919 shows expanded choice audit/source records.


### Next increment — product scope and future template receipts

Implemented locally: v7 requires structured product scope and exact written claim citations; absence of a product claim cannot coexist with a missing-SKU unknown verdict. Conflicts invalidate the response, never turn it into a pass. Photographic text is labeled model-observed separately from exact caption/registered overlay text.

Future Reposado previews now capture actual draw-time text/geometry/font-load observations, hash/render the same source Blob, and save through authenticated marketing-branded-save. Additive0131 stores browser-declared receipt plus server source/output hashes; atomic attach preserves originals, clears prior audit/schedule, protects publishing/published, retains planner originals for trust corrections, and reuses request IDs on retries. Unknown template outputs never inherit the reviewed26registry. Saved browser declarations remain untrusted to the model and UI; they are not proof that words were applied to pixels. Historical renderer archived byte-for-byte so current renderer changes do not invalidate reviewed historical exports.

Local validation:2,968root tests passed `/tmp/anejo-v7-release-root.log`; lint0errors/11existingwarnings `/tmp/anejo-v7-release-lint.log`; Functions build passed `/tmp/anejo-v7-release-build.log`;4renderer contract tests separately passed `/tmp/anejo-final-render-contract.log`. Tests include actual UI handler execution, sameBlob hash/render, explicitUse, idempotent retry, real SQLite attach/races, preserved source formats/suffixes, false declaration remaining untrusted, and audit source-read failure. No provider call, newcustomercommunication or production migration yet for this increment. Next: release checks, additive migration, deployment and live private workflow verification. Scope still open for stronger rendering attestations and autonomous team operation.
