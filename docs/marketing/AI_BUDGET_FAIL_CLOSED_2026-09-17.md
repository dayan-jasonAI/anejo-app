# AI budget evidence unavailable — local repair

Changes: `functions/_lib/ai_budget.js` now returns null (not zero) when the weekly ledger cannot be read, its binding is missing, or its aggregate is not a nonnegative safe integer. `budgetGate`/`underBudget` refuse with `budget_unavailable`, unknown spent/remaining null. A real numeric zero SUM remains valid. At the recorded ceiling the existing reached refusal remains.

Owner automation-budget API and both Marketing budget displays preserve unknown values explicitly; no $0 or available remainder is inferred. Owner Ask/operator, Creative Studio message/stream, marketing enhancement, sales brief/followup messages now distinguish unavailable evidence from exhausted budget. Other assigned agents update Team Lead/governance/automation consumers. Marketing retains an unsaved Lead reply only ephemerally with “Not saved—do not repeat actions blindly”; menu counts say menu items.

Local validation: 18 budget/UI/SQLite-reader tests and85 affected social/image fixture tests; final suite owned by parent. Explicit missing-ledger image test proves no provider invocation. Fixtures repaired to supply their intended numeric SUM0, not by restoring fail-open application logic. Logs: `/tmp/anejo-budget-focused.log`, `/tmp/anejo-budget-fixture-fix.log`; first full-run failure discovery `/tmp/anejo-budget-failclosed-full.log`. Scoped ESLint and diff checks pass. No provider requests, production DB writes or deployments performed in this subtask.

Remaining limits: ledger writes are still best effort and post-call; no durable reservation/atomic concurrent spend allocation, reconciliation with provider invoices, fixed maximum per-call cost reservation, or price-table freshness verification is added. A readable ledger can therefore still undercount prior failed writes and simultaneous in-flight calls. Do not describe this repair as proof of an absolute invoice ceiling. Production behavior remains Unverified until parent release and live checks.
