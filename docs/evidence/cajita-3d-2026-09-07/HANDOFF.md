# Cajita configurator — working draft, not production-ready

Date: 2026-09-07. Owner: Codex, direct Dayan session request. Branch: `codex/cajita-3d-builder`.

## Authorization and release state

Dayan's direct instruction authorizes custom food selections, duplicates/removals, multiple event versions, theme/colors, original-brand preservation, manual design placement, AI concepts, box research, backend detail capture, and implementation. No purchases were authorized or made. The original packaging measurements were not supplied.

**Open — evidence missing** for production completion. Do not merge this branch or deploy its application to production yet. The public `/cajita` and `/catering` pages were not changed in this work stretch. No production database records, emails, secrets, DNS, or settings were changed for the configurator.

## Created / modified

- `public/cajita-builder.html`, `public/assets/css/cajita-builder.css`, `src/cajita/*`: local 3D design prototype and versioned quote form.
- `scripts/build-cajita.mjs`, npm `build:cajita`, Three.js/esbuild dependencies, committed browser bundle.
- Seven native mesh models: sandwich, empanada, croqueta, salad, grazing, tres leches, grape/ham/guava/cheese/pineapple skewer. These are deliberately labeled illustrative. They do NOT meet the requested realistic-food quality bar yet.
- Six standard foods include tres leches. Skewer is an optional add-on. Each item can be zero or duplicated. Schema allows 20 versions, 1–5,000 boxes/version, 0–50 of each item/box; larger requests have explicit notes. Empty boxes/duplicate IDs are rejected.
- 20 occasion palettes, unrestricted color pickers, decorative patterns, topper shapes; manually place JPG/PNG and personal text on labels/tags/picks. Original wordmark/emblem source pixels are composited, never regenerated by AI. Dark background/counter pixels are excluded before recoloring so the lettering remains open.
- Local opt-in IndexedDB save includes artwork blobs and configuration, excludes contact fields. Downloadable JSON contains all selections and placement coordinates but not the binary artwork.
- `functions/_lib/cajita-config.js`, public leads integration, owner catering API/UI and kitchen API/UI: full JSON plus production summary in existing `leads.message`, attachment ownership validation, exact variant counts and ingredient totals. Kitchen omits customer contact PII; owner-only attachment downloads unchanged.
- AI theme endpoint scaffold with bounded request body, strict unavailable/error handling, D1 reservation, and explicit default-OFF feature flag. It is NOT enabled or production-validated.

## Evidence / validation

- `npm run build:cajita`: exit 0.
- `npm run lint`: exit 0 (existing lint scope: functions, tests, scripts).
- `npm test --silent`: 1,784 tests passed, 0 failed, exit 0, measured in this session.
- `git diff --check`: exit 0.
- Additional backend evidence: `BACKEND_AUDIT.md` (earlier 1,779-test run, before the five model/catalog tests).
- Browser local URL: `http://localhost:8788/cajita-builder`.
- Browser interaction proof: 20 Classic + 10 No dessert, with 2 skewers per dessert-free box. Displayed 30 boxes, 30 of each savory standard food, 20 tres leches, 20 skewers. First implementation missed quantity edits; changed to validated input handling and reran successfully.
- Changed second version to Halloween / star pick; displayed correct version theme without changing first version or item counts.
- Added generated skewer PNG to label, edited text to “Happy Birthday,” saved locally and reloaded; both variants and exact totals restored. Original file retained locally in saved draft; no submission or production email performed.
- Mobile viewport 390×844 initially exposed horizontal overflow; min-width/grid fix applied. Retest measured document width=390 and viewport width=390, with food stepper controls visible.
- Browser saw a deprecated shadow-map warning; replaced with supported PCFShadowMap and rebuilt. No observed browser error entries during this interaction run.
- Mesh tests cover finite coordinates/bounds, quantity math, palette colors, protected food colors and external texture disposal. Standard six plus skewer measured 11,850 triangles.

## Real 3D generation attempt — blocked externally

Two direct image-to-3D conversion attempts returned `400 Bad Request / Failed to generate 3D model`; neither returned a job ID or model file. First used the existing Cajita editorial image. Second used a clean isolated skewer reference, independently checked HTTP 200 and image/png.

Built-in ImageGen produced `public/assets/img/cajita/models-reference/skewer-reference-v1.png` (1,034,348 bytes). It is a generated reference, not an actual product photograph and not a 3D model. Original source remains under `.codex/generated_images/01a0722b-6149-71e1-a30d-beb0982de996/exec-064fa6c0-9338-481b-a93b-63fe5d1d65a3.png`.

Only this reference and a noindex description were staged at a non-production static preview to provide a fetchable URL for conversion: `https://b6b64fef.anejo-app.pages.dev/skewer-reference-v1.png` (branch alias `cajita-food-reference.anejo-app.pages.dev`). No application functions were included in that isolated staging directory. Public production domain was not deployed.

ImageGen prompt: “Use case: product-mockup. Generate ONE isolated appetizer skewer reference photo for later real 3D reconstruction of Añejo Catering Co food. Reference photo shows their real clear party box; use its grape and folded meat grazing bite as texture/scale reference. Subject is a single short wooden cocktail skewer threaded with ONE realistic green grape, delicately folded pink ham, one small ruby guava paste cube, one pale cheese cube, and one fresh golden pineapple cube. Entire skewer visible. Polished realistic studio product photo at 30-degree elevated side angle, centered on pure white background, soft neutral light, crisp food textures, natural moist highlights, gentle contact shadow. No plate, no box, no label, no text, no invented logo, no hand, no illustration. Preserve appetizing real food appearance.” Reference: `public/assets/img/cajita/pink-first-birthday-cajita-close.jpg`.

## Remaining risks / exact next work

1. Realistic food assets: conversion service unavailable; current native meshes are too stylized. Produce and visually approve real textured models before release. Do not portray reference images as glTF assets.
2. Original Amazon box: **Needs Dayan confirmation**. Ask for order link/product name/dimensions. Research candidate only: APSIC clear PET SZ family, https://www.apsicpackaging.com/products/sz-11, but NOT established as original or suitable for hot food. Do not infer capacity or buy. Larger packaging remains review-only; scene scales conceptually, never a physical packing guarantee.
3. Public AI themes: disabled. Verify spend for all attempted/fallback providers, strict shared budget concurrency, content output suitability, cleanup and retention before enabling. Existing ledger helpers swallow spend write failures; reservation conservatively retained. Do not assert a proven hard financial cap.
4. PDF upload currently attaches instructions only; it is NOT positioned/rasterized in the manual editor. JPG/PNG works. Broader artwork/text typography/layout capabilities and preview parity with print production need review.
5. Live end-to-end proof still required: submission persisted, attachment ownership/linking, owner and kitchen views, alert receipt and email delivery (not just provider acceptance), retry/idempotency, deliberate provider failure paths. Tests do not establish real inbox delivery.
6. Lead route lacks full request-idempotency; frontend warns about uncertain network receipt and does not silently mailto away a design. Add a durable idempotency contract before production release.
7. Scene caps visible food meshes at 28 while preserving all requested counts; UI discloses first-N shown. Physical dimensions, practical limits and layout packing remain unverified. Some input/scene changes rebuild geometry; tune before mobile release.
8. Keep new builder unlinked from live navigation until all required release checks pass. No production approval is being requested a second time: these are missing proof/quality, not a duplicate permission gate.

## Continuation and rollback

While model conversion and packaging facts were blocked, completed local state/interaction, schema, kitchen handoff, upload-reference and mobile-layout work. All changes are isolated to this feature branch (workspace auto-sync also created `[skip deploy]` commits). Do not remove/revert unrelated work. No production rollback is needed for this draft. If abandoned, retain branch and evidence; do not delete user files.
