{
  "name": "anejo-app",
  "version": "1.0.0",
  "description": "Añejo Catering Co. — marketing site + AI macro calculator (public self-serve) + trainer portal. One Cloudflare Pages app.",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-05-01",
    "deploy": "wrangler pages deploy public --project-name=anejo-app",
    "build:studio": "npm --prefix hub-app run build && rm -rf public/studio && mkdir -p public/studio && cp -R hub-app/dist/index.html hub-app/dist/assets public/studio/ && find public/studio -name '*.map' -delete",
    "lint": "eslint functions test scripts",
    "test:auth": "node --no-warnings --test test/auth/*.test.js",
    "test:studio": "node --no-warnings --test test/studio/*.test.js",
    "test:money": "node --no-warnings --test test/money/*.test.js",
    "test": "node --no-warnings --test test/*.test.mjs test/auth/*.test.js test/studio/*.test.js test/money/*.test.js test/ui/*.test.js test/compliance/*.test.js test/telemetry/*.test.js test/marketing/*.test.js",
    "test:ui": "node --no-warnings --test test/ui/*.test.js",
    "predeploy": "npm test --silent && node scripts/predeploy-guard.mjs",
    "verify:deploy": "node scripts/verify-live-deploy.mjs",
    "postdeploy": "node scripts/verify-live-deploy.mjs",
    "verify:live": "node scripts/verify-live.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "globals": "^15.15.0",
    "wrangler": "^3.80.0"
  }
}
