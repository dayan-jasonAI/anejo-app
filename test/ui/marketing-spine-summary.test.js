import test from 'node:test';import assert from 'node:assert/strict';import vm from 'node:vm';import {readFileSync} from 'node:fs';
test('sidebar counts every catalog kind and retains snapshot date',()=>{const src=readFileSync(new URL('../../functions/api/hub/owner/team.js',import.meta.url),'utf8');const start=src.indexOf('function spineSummary(spine) {'),end=src.indexOf('\n}',start)+2;const ctx={};vm.runInNewContext(src.slice(start,end),ctx);const s=ctx.spineSummary({menu:[{available:true}],other_items:[{kind:'catering',available:true},{kind:'traditional',available:false}],metrics:{account:{followers:76,capture_date:'2026-09-15'}},drafts:{count:0},budget:{}});assert.equal(s.menu_total,3);assert.equal(s.menu_available,2);assert.equal(s.metrics_as_of,'2026-09-15');const html=readFileSync(new URL('../../public/hub/owner/marketing.html',import.meta.url),'utf8');assert.match(html,/followers · snapshot/);assert.match(html,/String\(s.metrics_as_of\)/);});
