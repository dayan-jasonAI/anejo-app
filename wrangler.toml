name = "anejo-app"
compatibility_date = "2026-05-01"
pages_build_output_dir = "public"

# Secrets/vars are set in the Cloudflare Pages dashboard (NOT here):
#   ANTHROPIC_API_KEY   (Secret, encrypted)   <- required for the AI calculator
